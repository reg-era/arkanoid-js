import { Vector2D } from "./vectore.js";

export class Paddle {
    constructor(width,speed) {
        this.width = width
        this.speed = speed
        this.position = Vector2D
    }

    move(direction) {
    }

    updatePosition(number) {
    }

    checkBoundary() {
    }
}